# Commands

```sqlite
# Mostra bases de dados
.databases

# bd para sql 
testDB.db .dump > testDB.sql

# sql -> bd
testDB.db < testDB.sql

# sair 
.quit

# mostra nomes das colunas
PRAGMA table_info(table_name)

```